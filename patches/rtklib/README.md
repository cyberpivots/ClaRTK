# RTKLIB Patch Policy

- Keep all local ClaRTK deltas against RTKLIB documented here.
- Prefer wrapper code in `core/solvers/rtklib-bridge` over invasive upstream edits.
- Any required upstream modification should include rationale, affected version, and rollback plan.

